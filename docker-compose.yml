version: '3.8'
services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./:/app
  collector:
    build: .
    command: ["python", "collector/run.py", "--date", "$(date +%F)", "--headless"]
    volumes:
      - ./:/app
